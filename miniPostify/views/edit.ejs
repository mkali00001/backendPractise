<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 p-2 text-sm inline-block rounded-md mb-3" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mb-5"><span class="font-light">Edit Your Post</h3>
        <form action="/update/<%= post._id  %>" method="post">
            <textarea id=""
                class="w-1/3 block resize-none p-3 outline-none border-2 h-52 border-zinc-700 bg-transparent rounded-md"
                placeholder="Write your thought's..." name="content"><%= post.content %></textarea>
            <input type="submit" class="px-3 py-2 text-sm bg-yellow-500 rounded-md mt-5 block" value="Update Post">
        </form>
    </div>
    </div>
</body>

</html>