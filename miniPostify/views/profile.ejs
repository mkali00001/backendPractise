<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 p-2 text-sm inline-block rounded-md mb-3" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mb-5"><span class="font-light">Hello</span>
            <%= name %> 👋🏻
        </h3>
        <h5 class="mb-5">You can create a new post.</h5>
        <!-- <p><%= username %>  <%= email %></p> -->
        <form action="/post" method="post">
            <textarea id=""
                class="w-1/3 block resize-none p-3 outline-none border-2 h-52 border-zinc-700 bg-transparent rounded-md"
                placeholder="Write your thought's..." name="content"></textarea>
            <input type="submit" class="px-3 py-2 text-sm bg-blue-500 rounded-md mt-5 block" value="Create Post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <%user.posts.reverse().forEach((post)=>{%>
                    <div class="w-1/3 p-4 rounded-md mb-4 border-2 bg-zinc-800 border-zinc-700">
                        <h4 class="text-blue-500">@<%= user.username %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %></small>
                        <div class="btns flex justify-between mt-2">
                            <a class="text-blue-500" href="/like/<%=post._id%>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike"%>
                            </a>
                            <a class="text-green-500" href="/edit/<%=post._id%>">Edit</a>
                        </div>
                    </div>
                <%})%>
            </div>
        </div>
    </div>
    </div>
</body>

</html>